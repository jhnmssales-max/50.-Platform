{
  "name": "referral-platform-api",
  "version": "0.1.0",
  "private": true,
  "description": "Referral platform API: dealer-facing endpoints (create a customer + invite link, list/search referrals) and public endpoints (resolve a link, generate a share link, submit a referral). No email sending, no gift card integration yet.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "nanoid": "^3.3.7",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  }
}
